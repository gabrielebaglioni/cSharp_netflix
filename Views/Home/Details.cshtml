
<h1>Dettaglio attore</h1>
<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="js_actor_container">
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    debugger;
    let id = window.location.pathname.split('/')[3];

    axios.get(`/api/home/get/${id}`).then(res => {

        const actor = res.data;
        document.querySelector('.js_actor_container').innerHTML +=
            `
            <div class="col-4 py-2" >
            @*<a href="@Url.Action("Details", "Home", new {Id = actor.id})" class="btn btn-danger my-3">Details*@
                <div class="card bg-danger text-black border-2  rounded-lg" >
                   <span class="py-1" >Name ${actor.name}</span>
                   <span class="py-1" >Surname ${actor.surname} </span>
                 </div>
            <div class="d-flex justify-content-center align-items-center gap-4">
                <a href="@Url.Action("Index", "Home")" class="btn btn-danger my-3">Home</a>
                <a href="@Url.Action("Edit", "Home")" class="btn btn-danger my-3">Edit</a>
                @*<button onclick="deleteActor(${actor.id})" class="btn btn-danger">Elimina</button>*@
            </div>
            `;
    }).catch(err => {
        console.log(err);
    })
    @*function deleteActor(id) {
        axios.delete('/api/home/delete/' + id).then(res => {
            console.log(res);
            loadList("");
            alert('scusa Paolo, mi hai eleminato :(');
        }).catch(err => {
            console.log(err);
            alert('Non posso elimanarla paolooooo!');
        })


    }*@

</script>